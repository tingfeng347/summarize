保留6位有效数字，四舍五入。123.456   23.4567

```java
double min=n/(h*h);
String ss[]=(min+"").split("\\.");
	    			String g="";
for(int i=0;i<6-ss[0].length();i++) {
	    				g+="#";}
后面小数点后的#有几个代表保留几位。
DecimalFormat df=new DecimalFormat("#."+g);
//取消后面多余的0；	    			df.setDecimalSeparatorAlwaysShown(false);
	    			String s=df.format(min);
	    			System.out.println(s);
```

//math.round(3.456)=>3直接四舍五入到整数了。

```java
//大数类型	特色（进制转换(常用的是Integer的方法)，求最大公约数）
int A=42;
BigInteger f=BigInteger.valueOf(A);//将数字转成大数类型的
//转为int
int s=f.intValue();
//转化为BigInteger类型的树；
BigInteger c=new BigInteger("6");
BigInteger d=new BigInteger("3");
c.add(d);//加
c.subtract(d);//c-d
c.multiply(d);//相乘；
c.divide(d);//c/d;
c.remainder(d);//c%d
c.pow(2);//求n次方
c.gcd(d);//最大公约数；
c.abs();//绝对值
c.negate();//x相反数
c.max(d);
c.equals(d);
int shu=c.compareTo(d);
```

```java
BigDecimal
       BigDecimal b1 = new BigDecimal("1");
		        BigDecimal b2 = new BigDecimal("2");
		        BigDecimal b3 = new BigDecimal("4");
		        System.out.println("相加："+b1.add(b2));+
		        System.out.println("相减："+b1.subtract(b2));-
		        System.out.println("相乘："+b2.multiply(b3));*
		        System.out.println("相除："+b2.divide(b3));/
                    double sa= s.add(a).setScale(1, RoundingMode.HALF_UP).doubleValue();
//两小数相加，四舍五入保留一位小数，并转化为小数。
```

